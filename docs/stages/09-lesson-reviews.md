# Stage 9 lesson review synthesis

Three independent read-only reviewer agents were requested through the workflow runner before HTML implementation. The background run did not return a retrievable report to this session. The lesson therefore does not claim a completed independent-agent review. The builder performed the three required review passes separately and recorded the resulting decisions below. This remains a validation boundary.

## Pass 1: prerequisite and pedagogy order

### Diagnosis

The main risk was opening with a field taxonomy or a 24-row chart. A newcomer first needs one visible string, one changed probability table, and one hand-worked count.

### Adopted order

1. Restore the exact Stage 8 rank 1000 string and its unedited, deleted, and paraphrased scores.
2. Define the checker as a test against one key and profile.
3. Rewind to Stage 3's saved first-token draw.
4. Show that `Jack` changes chance but remains the saved token in both paths.
5. Place that step inside the complete generation loop.
6. Replay rank 1000's first 80 copied tokens.
7. Calculate ordinary hits, ordinary movement, and z from concrete values.
8. Introduce the cutoff only after the score.
9. Add length, four controls, every paired row, natural-web crossings, edits, and bias trade-offs in that order.
10. Map the implemented analogue to KGW, SynthID-Text, and Anthropic's stated plan only after the mechanism is understood.

Hashes, package versions, source commits, cost arithmetic, and reproduction commands remain in disclosures or the article's final section.

## Pass 2: novice language and claim boundary

### Terms changed or defined

- "logit" became "candidate score" in the main path.
- "null" became "configured quarter-green average" before the formal statistical term appears.
- "standard deviation" became "usual movement" in the worked calculation.
- "score family" is introduced through four literal checked-text and key combinations.
- "conditional NLL" is named only beside the sentence that defines it as a model-based surprise measure.
- "bootstrap interval" stays attached to the frozen 24-document mean and is never called accuracy.

### Claim decisions

- Positive wording is exactly "consistent with this configured watermark and key."
- C4 is called natural-web text, not verified human writing.
- The 400 value is called a generated-token safety cap. The lesson reports zero complete 400 copied-token pairs.
- Embedding cosine, NLL, repetition, and assistant review do not become human quality judgments.
- Anthropic's mechanism and quality statements are attributed to Anthropic. The lesson says the public page does not disclose an algorithm this project could reproduce.

## Pass 3: narrative and interaction

### Controls kept

- score-increase off/on for one saved candidate table;
- Play, Pause, Previous token, Next token, and Replay for the copied-token checker;
- fixed Stage 1 length presets;
- four checked-text and key combinations;
- three 24-row paired contrasts;
- eight named edit conditions; and
- delta 1, 2, and 3 presets.

Each interaction states the fixed inputs, changed value, item to watch, and result sentence. Free text, key entry, arbitrary attack sliders, and model generation controls were excluded because the page uses frozen evidence.

### Continuity decisions

Rank 1000 remains the named object from opening through checker, control comparison, and edit return. Token colors come from selected token evidence and reconcile with `28/79`. Matching generated paths are not aligned after their histories diverge. Edit score change, retained length, and preservation status remain separate.

## Screenshot findings

Four context-free mid-page screenshots were inspected:

1. The sampler screenshot shows the fixed fields, five candidates, changed chance, saved `Jack` result, and complete loop.
2. The mobile checker screenshot keeps the token strip, controls, four concrete quantities, and cutoff explanation readable in one vertical sequence.
3. The cohort screenshot identifies zero, every row, the mean interval, and the natural-web crossing count without relying on earlier sections.
4. The editing screenshot identifies the selected edit, all 12 paired changes, and the preservation warning.

No critical narrative or claim-boundary issue remained after these passes. Independent reviewer output remains the only missing review gate.
