# Claims ledger

Substantive claims are labelled as `external`, `derived`, `measured`, or `opinion`. Measured
claims remain pending until their committed artifact exists.

| Category | Statement | Source or artifact | Status | Allowed article wording |
|---|---|---|---|---|
| derived | Under the idealized independent null, green hits follow `Binomial(T, gamma)`. | `src/watermark_lab/stats.py`; fixed tests in `tests/unit/test_stats.py` | Verified | State only with the independent-trial qualification. |
| derived | The expected count is `T * gamma`, and the displayed z-score standardizes the excess. | `src/watermark_lab/stats.py`; fixed tests in `tests/unit/test_stats.py` | Verified | Describe as the configured detector statistic. |
| measured | With the locked Stage 1 seed and configuration, simulated biased detection rises from 0.2133 at `T=40` to 1.0 at `T=400`; simulated null detection ranges from 0.0013 to 0.0021. | `artifacts/lab-01/summary.json`, source `e99e9e5f9b8bc426d1cc4e13f874854f8c303475` | Verified by `just verify-lab-01` | Use “simulated,” include the configured probabilities and threshold, and cite the artifact. |
| opinion | Limitation: the biased condition `p=0.40` is pedagogical and not an LLM measurement. | `configs/lab_01.toml` | Approved scope statement | Do not derive it from a future logit bias. |
| opinion | Limitation: real token histories can be dependent, repeated, and miscalibrated relative to the ideal binomial model. | Canonical algorithm notes | Approved limitation | Present as a limitation, not a measured Stage 1 result. |
| derived | The locked Stage 2 toy rule selects exactly 5 of 20 token IDs for each context. | `configs/lab_02.toml`; `src/watermark_lab/toy_greenlist.py`; frozen vector tests | Verified | Call this a property of the toy rule, not a property of an upstream KGW implementation. |
| derived | Adding `delta=2.0` to a green logit multiplies its odds relative to an unchanged red logit by `exp(2)`, about 7.389, before softmax normalization. | `src/watermark_lab/toy_greenlist.py`; `tests/unit/test_toy_greenlist.py` | Verified | State that final token probabilities still depend on every original logit and normalization. |
| derived | In the locked four-position trace, the choices after the boost are token IDs `[0, 1, 1, 2]`; checker replay counts 2 green hits in 4 eligible positions. | `artifacts/lab-02/trace.json`, source `f7a1690d28cfb48fc825017891b5d3e82eebdd07` | Verified by `just verify-lab-02` | Describe only this deterministic teaching trace. Do not report it as a detection rate. |
| opinion | Limitation: the public development key and SHA-256 selector are teaching devices, not a secure or upstream-compatible design. | `docs/stages/02-toy-vocabulary.md` | Approved scope statement | Keep this warning next to the toy trace and any interactive lesson. |
| external configuration | The selected Stage 3 fixture is `mlx-community/LFM2-350M-4bit` at revision `18dc72abf3b2337f9123cfd6eeeb58dfa7947066`, loaded with MLX-LM 0.31.3 and MLX 0.32.0. | `configs/lab_03.toml`; `artifacts/lab-03/trace.json` | Verified by `just verify-lab-03` | Call this a pinned local fixture, not a claim about current model quality. |
| derived | The Stage 3 `mlx-mix-v1` selector marks exactly `floor(0.25 * vocabulary_size)` token IDs green for one previous token ID and key. | `src/watermark_lab/manual_generation.py`; fixed-vector tests | Verified | Call this a portable lab profile, not a cryptographic or upstream-compatible design. |
| measured | In the selected three-passage run, same-key green counts were `8/39`, `10/39`, and `11/39` with the score increase off, and `21/39`, `26/39`, and `17/39` with it on. | `artifacts/lab-03/trace.json`, source `2f082b7f63853811881c0f23c2d7022e8e5dbc3b` | Verified by `just verify-lab-03` | Describe only this paired local run. Do not infer accuracy, prose quality, or a cutoff. |
| opinion | Limitation: a Stage 3 z score is descriptive evidence for one configured key and trace. It is not an AI-origin or authorship decision. | `docs/stages/03-manual-generation.md` | Approved scope statement | Say “consistent with this lab watermark” only after a later tested cutoff exists. |
| derived | In the locked Stage 3 order, adding `2.0` before temperature `0.8` multiplies green-versus-red relative odds by `exp(2 / 0.8)`, about `12.1825`, before top-p and top-k. | `configs/lab_03.toml`; `src/watermark_lab/manual_generation.py`; fixed sampling-order tests | Verified | State that top-p, top-k, normalization, unequal raw scores, and changing histories still determine the final probabilities. |
| measured | At the first continuity position, both paired rows begin with the same raw score list and seed. `Jack` has an `11.6422%` final chance with the increase off and `18.5816%` with it on; the recorded draw selects `Jack` in both rows. | `artifacts/lab-03/trace.json`, source `2f082b7f63853811881c0f23c2d7022e8e5dbc3b` | Verified by `just verify-lab-03` | Use this to show that different probability tables can produce the same sampled token. Do not infer quality. |
| opinion | Limitation: Stage 3 changes the sampling distribution but does not measure whether the change affects coherence, usefulness, diversity, or downstream accuracy. | `docs/stages/03-manual-generation.md`; three fixed prompt pairs | Approved scope statement | Say “quality was not measured,” never “quality was preserved” or “quality was degraded.” |
| external | KGW-style green-list score increases improve watermark strength by changing the sampling distribution; stronger settings can increase text distortion. | [Kirchenbauer et al.](https://arxiv.org/abs/2301.10226); [Fernandez et al.](https://arxiv.org/abs/2308.00113) | Primary papers reviewed | Present as literature context. Do not transfer paper quality results to the Stage 3 model or sampler. |
| external | Non-distortionary SynthID-Text preserves a stated output distribution on average over watermark randomness and reports no detected quality loss in its tested production, controlled-human, perplexity, and benchmark evaluations. | [Dathathri et al.](https://www.nature.com/articles/s41586-024-08025-4) | Primary paper reviewed | State the averaging/configuration conditions, the observed inter-response diversity reduction, and that Stage 3 does not implement SynthID. |
| external | Anthropic says its supported Claude text marks do not change meaning, quality, or readability, while its public support page leaves the algorithm and technical evaluation undisclosed and says detection details are forthcoming. | [Anthropic support](https://support.claude.com/en/articles/16266773-how-claude-marks-ai-generated-content) | Current vendor page reviewed 2026-08-13 | Attribute this to Anthropic. Do not present it as a reproduced or independently validated Stage 3 result. |
| external configuration | The Stage 4 reference fixture is `openai-community/gpt2` revision `607a30d783dfa663caf39e06633721c8d4cfcd7e` with Transformers 5.14.1 and PyTorch 2.13.0 on the local CPU. | `configs/lab_04.toml`; `artifacts/lab-04/trace.json` | Verified by `just verify-lab-04` | Call GPT-2 an implementation fixture, not evidence about current model quality. |
| derived | Transformers 5.14.1 applies temperature, top-k, top-p, and then its watermark processor in the locked generation path. The saved Stage 3 order changes scores before temperature, top-p, and top-k. | locked Transformers source; `src/watermark_lab/hf_adapter.py`; fixed order tests | Verified | State that the four causal parts correspond but the complete recipes are not equivalent. |
| measured | On the saved first GPT-2 preference list, the Transformers order leaves 19 choices and gives token ID 373 an `8.642730%` chance. The Stage 3 order calculated on the same list leaves 11 choices and gives it an `8.825517%` chance. | `artifacts/lab-04/trace.json`, source `20b4860e0d64ca116b173bc42f971d50eb0fef95` | Verified by `just verify-lab-04` | Say only the reference order generated the saved continuation. Do not say one order is better. |
| measured | Stage 4 generation-key counts for the three reference-watermarked continuations are `17/39`, `21/39`, and `22/39`, with z scores `2.6811`, `4.1603`, and `4.5301`. | `artifacts/lab-04/trace.json`, source `20b4860e0d64ca116b173bc42f971d50eb0fef95` | Verified by `just verify-lab-04` | The configured result is positive only when z is strictly greater than 3. Use "consistent with this lab watermark and key," never AI or authorship detection. |
| measured | For a six-token alternating fixture, both pinned library repetition modes score `3/5` with z `1.8074`, while explicit value-based distinct pairs score `1/2` with z `0.8165`. | `artifacts/lab-04/trace.json`, source `20b4860e0d64ca116b173bc42f971d50eb0fef95` | Verified by `just verify-lab-04` | Call this a fixed compatibility mismatch in Transformers 5.14.1, not universal library behavior. The fixture is not model output. |
| opinion | Limitation: three GPT-2 passages on one local CPU do not measure detection accuracy, a false-alarm rate, prose quality, device portability, or a generally useful cutoff. | `docs/stages/04-transformers-reference.md` | Approved scope statement | Keep the local profile, small sample, and cutoff qualification beside Stage 4 results. |
| external configuration | The Stage 5 smoke used `google/gemma-4-E2B-it` revision `3e22461f65e89153144f8adb70e3b8c2cc9845a7`, BF16, Transformers 5.14.1, PyTorch 2.13.0, and one Modal NVIDIA L4. | `configs/lab_05.toml`; pinned model card; `artifacts/lab-05/trace.json` | Verified by `just verify-lab-05` | Call this one pinned open-model runtime, not a stand-in for Claude or a model-quality comparison. |
| measured | The Stage 5 model downloaded in 36.739 seconds, loaded to CUDA in 5.782 seconds, and reserved at most 9.682 GiB of the recorded 22.034 GiB L4 memory, leaving 56.1 percent headroom. | `artifacts/lab-05/trace.json`, source `09831ba8f960d7070b67b9b5350cdffbae9b4c4d` | Verified by `just verify-lab-05` | State these as one disposable Modal invocation. Do not generalize cold-start or memory behavior to another image, model, or GPU. |
| measured | The three Stage 5 watermarked rows ran at 18.422, 18.747, and 19.259 generated tokens per second. Separate synchronized replays spent 7.165, 5.373, and 5.898 milliseconds in the maintained watermark processor over each continuation. | `artifacts/lab-05/trace.json`, source `09831ba8f960d7070b67b9b5350cdffbae9b4c4d` | Verified by `just verify-lab-05` | Keep generation wall throughput separate from synchronized component timing. Do not claim a speedup from the first control row. |
| measured | The three Stage 5 watermarked copied continuations scored `11/26` (z `2.0381`), `7/20` (z `1.0328`), and `9/22` (z `1.7233`) with the generation key; none crossed the strict `z > 3` cutoff. | `artifacts/lab-05/trace.json`, source `09831ba8f960d7070b67b9b5350cdffbae9b4c4d` | Verified by `just verify-lab-05` | Preserve the below-cutoff outcome. Six short rows do not estimate detection accuracy, and a miss does not imply human or unwatermarked origin. |
| derived | At the slowest measured watermarked rate and Modal's recorded L4 price of `$0.000222/s`, 9,600 generated tokens project to 521.1 seconds and `$0.1157`; 19,200 project to 1,042.2 seconds and `$0.2314`. | `artifacts/lab-05/trace.json`; `https://modal.com/pricing` retrieved 2026-08-15 | Verified by `just verify-lab-05` | Call these GPU-only linear generation projections. List excluded image, download, load, CPU, memory, storage, retry, and scaling costs. |
| opinion | Limitation: six Gemma generations on one L4 do not measure detector accuracy, prose quality, false-alarm rate, total cloud cost, cross-device portability, or a generally useful cutoff. | `docs/stages/05-gemma-modal-smoke.md` | Approved scope statement | Keep this boundary beside every Stage 5 summary and projection. |
| checked implementation | The Stage 5 shared core supports compatible Transformers generation models through an explicit adapter that supplies prompt encoding, text model config, generated-ID slicing, assistant-content extraction, copied-text tokenization, and device tensors. | `src/watermark_lab/transformers_runtime.py`; `src/watermark_lab/gemma_adapter.py` | CPU-only fake and adapter tests | Say compatible Transformers generation models, not every Hugging Face repository or arbitrary remote model. |
| checked implementation | The control and watermarked generation calls share all model, prompt, sampling, and output fields; the watermarked call alone receives `watermarking_config` built from the process-local key profile. | `src/watermark_lab/transformers_runtime.py` | `tests/unit/test_transformers_runtime.py` | This describes the maintained generation boundary, not a post-generation text transform. |
| opinion | The committed Stage 5 key is a public reproducibility fixture and provides no secrecy. A hosted private key should be injected at process startup and only a non-secret version label may appear in public response metadata. | `src/watermark_lab/key_policy.py`; `src/watermark_lab/service_contract.py`; `docs/stages/05-hosting-blueprint.md` | key-policy tests and schema inspection | This is a hosting blueprint, not a deployed endpoint or complete production key-management system. |
| measured | The Stage 5 implementation demonstration produced ten fixed control/watermarked pairs on the pinned Gemma/L4 path. None of the twenty rows crossed strict `z > 3`; watermarked z was higher in seven pairs, lower in two, and equal in one. | `artifacts/lab-05/examples.json`, source `b803245b44a8114f7a79ab29cf613a426ff59bdd` | `just verify-lab-05-examples` | Ten pairs demonstrate the implementation. They do not estimate detection accuracy, a false-alarm rate, quality preservation, or a production threshold. |
| measured | The selected examples report generation-key all-occurrence z, p-value, and strict decision for every copied continuation. The p-value is the configured no-watermark probability of evidence at least this extreme. | `artifacts/lab-05/examples.json` | Local z recomputation, p-value bounds, and decision checks | Never call the p-value "detection probability" or the probability that text is watermarked. |
| measured | In the frozen natural-length ladder, 8 of 12 watermarked rows and 0 of 12 control rows crossed strict `z > 3`. Watermarked z ranged up to `8.0271`; achieved copied lengths ranged from 200 to 800 tokens under normal end-token behavior. | `artifacts/lab-05/lengths.json`, source `dc871afde42239b8a7ca18b84745da9d92f2408a` | `just verify-lab-05-lengths` | Prompts and achieved lengths vary together. Do not call this a causal length effect, accuracy estimate, or calibrated detection rate. |
| measured | Every eligible copied token in the natural-length artifact records its exact Gemma token piece and generation-key green/red decision; the first copied token is unscored context. Token-level totals equal each row's detector `G/T`. | `artifacts/lab-05/lengths.json` | Local token identity, eligibility, count, and z verification | Green is keyed membership, not semantic quality or proof of AI origin. |
| external configuration | Stage 6 used the `allenai/c4` `realnewslike` validation shard at revision `1588ec454efa1a09f29cd18ddd04fe05fc8653a2`. The pinned compressed shard has SHA-256 `42ac859dc1c4d48d165ec602909403e2066ce1d4854149ed70b9ec9cc96dc65f` and declares 13,863 rows. | `configs/lab_06.toml`; pinned C4 dataset card and shard | Verified by download hash and `just verify-lab-06` | Call C4 natural-web text. Do not call every row verified human writing. |
| measured | The deterministic Stage 6 selector scanned 2,479 rows, rejected 1,451 as too short and four as obvious lists, selected 1,000 calibration rows, and froze the next 24 passing rows for paired testing. | `data/manifests/lab-06-c4.jsonl`; `artifacts/lab-06/calibration.json`, source `0508f0e084f94a98ac88abd22245b8ec81916b60` | `just verify-lab-06` | State that calibration filled first and score did not affect split selection. |
| measured | Four of 1,000 natural-web continuations crossed strict `z > 3` under Stage 5-compatible all-pair counting. The cohort median was `0.0289`, its 99th percentile was `2.4568`, and its maximum was `3.7286`. | `artifacts/lab-06/calibration.json`, source `0508f0e084f94a98ac88abd22245b8ec81916b60` | Every row's counts, z, exact tail, decision, and summary are reconstructed by `just verify-lab-06` | Call `4/1000` an observed count in this pinned cohort, not a production false-alarm rate. |
| measured | The maximum natural-web row scored `132/399`, z `3.7286`, with every adjacent pair counted. Counting each distinct value-pair once changed it to `114/358`, z `2.9904`. Across the cohort, one distinct-pair row crossed the cutoff. | `artifacts/lab-06/calibration.json` | Token/count reconciliation for the spine; independent score and summary reconstruction for all rows | Repetition policy is part of the detector profile. Do not claim that one policy is universally correct. |
| opinion | Limitation: 1,000 C4 natural-web rows cannot validate one-in-100,000 behavior, establish verified human authorship, define a production cutoff, or show separation from future paired model outputs. | `docs/stages/06-natural-web-calibration.md`; `blog/notes/06-natural-web-calibration.md` | Approved Stage 6 claim boundary | A positive result means only "consistent with this configured watermark and key." |
| measured | Stage 7 generated control and watermarked Gemma continuations for all 24 prompts frozen in Stage 6. The one Modal L4 invocation made exactly 48 generation calls and returned 12,933 generated token IDs without a Secret, Volume, endpoint, replacement prompt, or retry. | `artifacts/lab-07/results.json`, source `6b911ce4052794bfb6762859847e5cdcc197c165` | Verified by `just verify-lab-07` | Describe one bounded invocation on the pinned Gemma and L4 profile. Do not generalize its runtime or output lengths. |
| measured | All 24 pairs supported copied-token prefixes 40 and 80, 21 supported 160, 17 supported 200, and none supported 400 in both generated conditions. | `artifacts/lab-07/results.json` | Complete-prefix membership reconstructed by `just verify-lab-07` | Show each denominator. Never present the 400 generated-token safety cap as an achieved 400 copied-token result. |
| measured | At the 80 copied-token prefix, correct-key marked z exceeded paired model-control z by a mean `1.8296` with 95 percent paired bootstrap interval `[1.3424, 2.3276]`; it exceeded natural-web z by `1.7538` `[1.3100, 2.1977]` and comparison-key z by `2.0461` `[1.6131, 2.4792]`. | `artifacts/lab-07/results.json` | Every row difference, deterministic bootstrap seed, and interval reconstructed by `just verify-lab-07` | State that these intervals summarize 24 frozen documents. Do not call them accuracy, a population guarantee, or a calibrated error rate. |
| measured | At 200 copied tokens, 17 complete pairs remained. Four marked rows and one natural-web row crossed strict `z > 3`; no paired model-control or comparison-key row crossed. | `artifacts/lab-07/results.json` | Score counts, decisions, and complete-prefix cohort reconstructed locally | Keep the natural-web crossing and the reduced denominator visible. A crossing means only "consistent with this configured watermark and key." |
| measured | The fixed spine row scored `58/159`, z `3.3424`, on marked text with the generation key at 160 copied tokens. Its paired model control scored z `1.3278`, natural-web continuation `0.5952`, and comparison-key replay `-1.9688`. | `artifacts/lab-07/results.json`, selection rank `1000` | Fixed-spine token evidence reconciles to every displayed count | This is one predeclared row, not a representative accuracy claim. |
| measured | The predeclared inconvenient-row rule selected rank `1001`. Its control and marked paths shared their first 80 copied token IDs and therefore both scored `26/79`, z `1.6239`, with the generation key. | `artifacts/lab-07/results.json`; raw token identities in ignored Stage 7 return | `just verify-lab-07` and exact prefix identity check | Use this to show that changing sampling probabilities does not force a different sampled token or immediate separation. |
| derived | The Stage 7 function ran for 743.1 seconds. At the configured L4 rate of `$0.000222/s`, this corresponds to `$0.1650` of GPU time. | `artifacts/lab-07/results.json`; `configs/lab_07.toml` | Direct calculation from recorded runtime and configured rate | Call this a GPU-only derived amount, not the provider's total bill. List excluded CPU, memory, image, transfer, and provider overhead. |
| opinion | Limitation: the 24-row Stage 7 experiment does not establish production accuracy, a universal cutoff, quality preservation, editing robustness, human authorship, arbitrary AI origin, or Claude's private watermark design. | `docs/stages/07-paired-core-experiment.md`; `blog/notes/07-paired-core-experiment.md` | Approved Stage 7 claim boundary | Keep model, tokenizer, device, key, corpus, and small-cohort qualifications beside results. |
| measured | On the first twelve frozen Stage 7 marked outputs, mean correct-key z change at 80 copied tokens was `0.0000` after normalization, `-0.3248` after 10 percent word deletion, `-0.9960` after 30 percent deletion, `-0.6712` after 25 percent mixing, `-1.3424` after 50 percent mixing, and `-1.7105` after paraphrase. | `artifacts/lab-08/results.json`, source `a42b1b3c65f32ff80bf82a89b6f8888c8b272125` | Every edit and token score rebuilt by `just verify-lab-08` | State that these are paired score changes on twelve frozen texts. Do not call deletion or mixing meaning-preserving. |
| measured | One and five percent deterministic homoglyph substitution changed mean z by `-0.0217` and `-0.9311`; mean copied-token length ratios were `1.0448` and `1.2183`. | `artifacts/lab-08/results.json` | Exact character substitutions, code points, tokenizer output, and scores reconstruct locally | Call this a Unicode/tokenizer resilience diagnostic, not semantic paraphrase robustness. |
| measured | All twelve paraphrases passed the automatic length, number, and embedding-cosine screen. A non-independent assistant review marked ten pass and two uncertain; all ten passed rewrites reduced z, and no paraphrase crossed strict `z > 3`. | `artifacts/lab-08/results.json`; `data/reviews/lab-08-paraphrase.json` | Automatic fields reconstruct locally; manual decisions retain reviewer and notes | Say the review was non-independent. Do not treat embedding cosine or assistant review as proof of equal meaning. |
| measured | On eight frozen prompts, mean correct-key z at 80 copied tokens was `0.2923`, `2.1761`, and `2.4684` for delta 1, 2, and 3. Strict crossing counts were `0/8`, `1/8`, and `3/8`. Two rows had lower z at delta 3 than delta 2. | `artifacts/lab-08/results.json` | Every row's generated text, token evidence, and score reconstruct locally | Report raw rows and the non-monotonic cases. Do not claim a universal optimal bias. |
| measured | Mean conditional NLL was `0.5004`, `0.5415`, and `0.5783`, while mean repeated adjacent-pair fraction was `0.0373`, `0.0471`, and `0.0483` for delta 1, 2, and 3. | `artifacts/lab-08/results.json` | Metrics recomputed from recorded token IDs and pinned Gemma outputs | Call NLL and repetition model-based proxies, not human quality judgments. |
| opinion | Limitation: the Stage 8 fixture does not establish adaptive security, universal edit robustness, human-perceived quality, production calibration, another model or tokenizer, another watermark family, generic AI origin, authorship, or Claude's private watermark design. | `docs/stages/08-editing-and-bias-tradeoffs.md`; `blog/notes/08-editing-and-bias-tradeoffs.md` | Approved Stage 8 claim boundary | Keep the 12-row edit and 8-row bias denominators beside every conclusion. |
