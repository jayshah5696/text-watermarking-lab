# Article Phase 2 and later storyboard

## The key and one sentence

1. Keep the coin-to-token mapping on screen long enough to introduce a vocabulary of 20 words.
2. Show `Early one morning Jack` as the fixed context. Keep candidate cards in token order.
3. Apply the lesson key. Five cards acquire green hatching and the label `favored`.
4. Switch to the comparison key. The context and five-of-20 fraction stay fixed while eight memberships change.
5. Return to the lesson key. Reveal the exact hash material for `went`, its prefix `01d63f53`, and rank 1.
6. Keep the candidate cards in place. Add `2` to the selected scores, redraw their probability shares, and run the fixed `0.30` draw across the cumulative strip.
7. Show the counterfactual and marked outcomes side by side. The same draw selects `walked` before and `went` after.
8. Move the four-word window. Preserve the sentence and append `went`.
9. Step through `up`, `the`, and `hill` with one control. Keep the completed choice's context and selected set visible until the next step begins.
10. Turn generation around. The checker walks through the finished sentence, rebuilds each selected set, and updates `G`, `T`, expected hits, and z.
11. End with `2/4`, expected `1`, z `1.1547`, no cutoff. Switch to the comparison key to show `0/4` without implying wrong keys always score zero.

## Real model scores

1. Keep the same Jack passage and show the full model input boundary.
2. Focus on the top five saved candidates. Use paired probability bars rather than a table that swaps all cells.
3. Toggle the score increase. Keep raw scores and candidate order fixed.
4. Track `Jack` from raw score `14.6875` to `16.6875` and chance `11.6422%` to `18.5816%`.
5. Place the fixed draw under both distributions. Both select `Jack`.
6. Continue to the copied continuation and checker result `21/39`, z `4.1603`, beside the comparison-key `7/39`, z `-1.0170`.

## Processor order

1. Draw the same five operations as movable blocks on two parallel rails.
2. Keep token ` was` attached to both rails.
3. Advance one operation at a time and show the surviving candidate count.
4. End with `8.642730%` versus `8.825517%`.
5. Separate the repeated-pair compatibility fixture into a second compact panel. Draw all five occurrences and then collapse equal values to two distinct pairs.

## Gemma

1. Move one request packet across request, adapter, generation, copied text, and checker boundaries.
2. Show where the key can and cannot appear.
3. Land on the three short marked rows. Draw each eligible count against the strict cutoff.
4. Keep all three misses visible. Explain the early end-token outcome instead of blaming the configured cap.
5. Follow with the 12-pair natural-length ladder as a separate measured result, with the content-length confound written beside it.

## Calibration

1. Start with the rank distribution or sorted score strip so all 1,000 rows read as a background, not scattered dust.
2. Mark the median, 99th percentile, cutoff, and four crossings.
3. Select the frozen maximum row. Preserve its token sequence while changing only the repetition rule.
4. Draw `132/399, 3.7286` transforming into `114/358, 2.9904`; annotate the 41 removed observations and 18 removed hits.

## One row and three controls

1. Put rank `1000` in the center as the object under test.
2. Branch to marked correct-key, model control, natural web, and marked comparison-key paths.
3. Show text source and key role separately so each control answers one visible alternative.
4. Preserve all four `G/T` and z values. Do not dim three values into illegibility.
5. Add rank `1001` as the immediate counterexample with identical first 80 copied IDs and equal z.

## The cohort

1. Keep all 24 rows labeled by rank on a horizontal paired-difference plot.
2. Show zero as a clear reference and place values on separate rows, not repeated vertical coordinates.
3. Add the mean and interval only after the 24 values are readable.
4. Switch among the three contrasts without changing row order.
5. Add a prefix completeness strip for 40, 80, 160, 200, and 400 copied tokens. Show `24, 24, 21, 17, 0` as achieved matched counts.

## Editing

1. Keep rank `1000` text visible before and after deletion or paraphrase.
2. For deletion, mark removed words in place before reflowing the string.
3. Retokenize and rebuild the checker history. Preserve `28/79`, `25/79`, and `26/79` as the recurring scores.
4. Show all 12 row changes in a labeled slope or dumbbell plot for the selected edit.
5. Keep copied-length ratio and paraphrase review status in a separate evidence line. Do not fold quality into z.

## Bias

1. Keep all eight row paths visible and labeled at the right edge.
2. Highlight ranks `1000`, `1001`, `1004`, and `1006` because they demonstrate rise, overlap, and non-monotonic paths.
3. Place mean z, NLL, and repeated-pair fraction on three aligned mini-plots so the trade-off is visible without reading a sentence of numbers.
4. Keep the proxy warning next to NLL and repetition.

## Method families

1. Use a shared generation/checking frame rather than four dashboard cards.
2. KGW shows a keyed subset and excess count.
3. SynthID-Text shows candidate tournaments and keyed correlation.
4. Claude shows only the disclosed SynthID family and leaves undisclosed settings blank.
5. Passive classification has no generation-time intervention.
6. The conclusion panel names what each method can support and what it cannot establish.
