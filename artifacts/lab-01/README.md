# Stage 1 selected artifacts

`just lab-01` intentionally generates the selected `summary.json`, SVG figure, and PNG figure
from a clean source commit. Ignored raw rows live below `runs/lab-01/` and are the recomputation
source for `just verify-lab-01`.

Do not edit generated evidence by hand. Before the first evidence run, only this explanation is
present.
