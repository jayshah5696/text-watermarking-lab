#!/usr/bin/env python3
# ruff: noqa: E501
"""Build the standalone Stage 7 lesson from selected evidence."""

from __future__ import annotations

import html as html_lib
import json
from pathlib import Path
from typing import Any, cast

ROOT = Path(__file__).resolve().parents[1]
ARTIFACT = ROOT / "artifacts/lab-07/results.json"
OUTPUT = ROOT / ".agent/diagrams/text-watermarking-stage-7-lesson.html"


def compact(value: object) -> str:
    return json.dumps(value, allow_nan=False, separators=(",", ":")).replace("</", "<\\/")


def text(value: object) -> str:
    return (
        html_lib.escape(str(value))
        .replace("\u2014", ", ")
        .replace("\u2013", "-")
        .replace("\u2026", "...")
    )


def main() -> None:
    artifact: dict[str, Any] = json.loads(ARTIFACT.read_text())
    rows = cast(list[dict[str, Any]], artifact["selected_rows"])
    spine = rows[0]
    inconvenient = next(
        row
        for row in rows
        if row["selection_rank"] == artifact["teaching_selection"]["inconvenient_selection_rank"]
    )
    spine_tokens = spine["token_evidence"]["watermarked_correct"]
    summaries = artifact["prefix_summary"]
    payload_rows = []
    for row in rows:
        payload_rows.append(
            {
                "rank": row["selection_rank"],
                "lengths": {
                    "control": row["conditions"]["control"]["copied_token_count"],
                    "watermarked": row["conditions"]["watermarked"]["copied_token_count"],
                },
                "prefix": row["prefix_scores"],
            }
        )
    payload = {
        "rows": payload_rows,
        "tokens": spine_tokens,
        "summaries": summaries,
        "spineControlIds": spine["conditions"]["control"]["copied_token_ids"],
        "spineWaterIds": spine["conditions"]["watermarked"]["copied_token_ids"],
        "inconvenientControlIds": inconvenient["conditions"]["control"]["copied_token_ids"],
        "inconvenientWaterIds": inconvenient["conditions"]["watermarked"]["copied_token_ids"],
    }
    runtime_seconds = artifact["runtime_ns"] / 1_000_000_000
    gpu_cost = runtime_seconds * 0.000222
    expected = 159 * 0.25
    movement = (159 * 0.25 * 0.75) ** 0.5
    html = f"""<!doctype html>
<html lang="en" data-theme="dark">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Stage 7: one mark, three controls</title>
<style>
:root{{color-scheme:dark;--bg:#000;--surface:#111;--raised:#181818;--border:#303238;--text:#f5f7fa;--muted:#a9adb5;--blue:#60a5fa;--cyan:#55d6e8;--green:#4ade80;--yellow:#facc15;--coral:#fb7185;--violet:#a78bfa;--track:#26282d}}
html[data-theme="light"]{{color-scheme:light;--bg:#f7f7f5;--surface:#fff;--raised:#eef0f2;--border:#c9cdd3;--text:#111318;--muted:#5f6570;--track:#dde1e6}}
*{{box-sizing:border-box}}html{{scroll-behavior:smooth}}body{{margin:0;background:var(--bg);color:var(--text);font:17px/1.58 system-ui,-apple-system,"Segoe UI",sans-serif}}header,main,footer{{width:min(1120px,calc(100% - 40px));margin:auto}}header{{padding:30px 0;border-bottom:1px solid var(--border)}}main{{padding-bottom:90px}}section{{padding:72px 0;border-bottom:1px solid var(--border)}}h1{{font-size:clamp(2.15rem,5.5vw,4.8rem);line-height:1;letter-spacing:-.05em;max-width:950px;margin:34px 0 22px}}h2{{font-size:clamp(1.6rem,3vw,2.6rem);line-height:1.12;letter-spacing:-.03em;margin:0 0 14px}}h3{{font-size:1.05rem;margin:0 0 8px}}p{{max-width:820px}}button{{font:inherit;min-height:44px;padding:.55rem .85rem;background:var(--raised);color:var(--text);border:1px solid var(--border);border-radius:6px;cursor:pointer}}button:hover{{border-color:var(--cyan)}}button.active,button[aria-pressed="true"]{{background:var(--text);color:var(--bg)}}button:disabled{{opacity:.38;cursor:not-allowed}}button:focus-visible,a:focus-visible,summary:focus-visible{{outline:3px solid var(--yellow);outline-offset:3px}}a{{color:var(--cyan)}}.top{{display:flex;justify-content:space-between;gap:16px;align-items:center}}.stage,.eyebrow{{font:700 .77rem/1.2 ui-monospace,monospace;letter-spacing:.11em;text-transform:uppercase;color:var(--cyan)}}.lede{{font-size:clamp(1.07rem,2vw,1.35rem);color:var(--muted);max-width:850px}}.question{{margin-top:24px;padding:15px 18px;border-left:4px solid var(--cyan);background:var(--surface)}}nav{{display:flex;flex-wrap:wrap;gap:12px;margin-top:22px}}nav a{{color:var(--muted);text-decoration:none;border-bottom:1px solid var(--border)}}.panel{{background:var(--surface);border:1px solid var(--border);padding:20px;border-radius:8px;min-width:0}}.grid2{{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin-top:24px}}.grid4{{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:18px}}.metric{{font-size:clamp(2rem,5vw,3.8rem);line-height:1;font-weight:760;letter-spacing:-.04em;margin:10px 0}}.metric small{{font-size:.35em;color:var(--muted)}}.controls{{display:flex;flex-wrap:wrap;gap:8px;margin:14px 0}}.try{{padding:14px 17px;border-left:3px solid var(--yellow);background:var(--raised);margin:22px 0 12px}}.try strong{{color:var(--yellow)}}.feedback{{padding:12px 14px;min-height:3.1em;border:1px solid var(--border);background:var(--bg);color:var(--muted)}}.frozen{{display:grid;grid-template-columns:repeat(24,1fr);gap:3px;margin:24px 0}}.frozen span{{aspect-ratio:1;background:var(--violet);border:1px solid var(--border)}}.frozen span:first-child{{outline:3px solid var(--yellow);outline-offset:2px}}.token-strip{{display:flex;gap:3px;overflow:hidden;padding:10px;border:1px solid var(--border);background:var(--bg);min-height:54px}}.block{{min-width:8px;height:30px;background:var(--blue);border:1px solid color-mix(in srgb,var(--blue) 70%,white)}}.fork{{display:grid;grid-template-columns:220px 1fr;gap:18px;align-items:center;margin-top:24px}}.paths{{display:grid;gap:12px}}.path{{padding:14px;border-left:4px solid var(--cyan);background:var(--surface)}}.path.marked{{border-color:var(--green)}}.fixed{{display:flex;flex-wrap:wrap;gap:7px;margin:10px 0}}.chip{{padding:4px 8px;border:1px solid var(--border);border-radius:999px;font:700 .78rem ui-monospace,monospace}}.diff{{background:color-mix(in srgb,var(--green) 18%,var(--surface));border-color:var(--green)}}pre{{overflow:auto;padding:16px;background:var(--bg);border:1px solid var(--border);font:13px/1.55 ui-monospace,monospace}}.history{{display:grid;grid-template-columns:1fr 1fr;gap:12px}}.history-lane{{min-width:0}}.history-line{{display:flex;gap:2px;overflow:hidden;min-height:38px;padding:7px;border:1px solid var(--border);background:var(--bg)}}.history-line i{{display:block;min-width:5px;height:22px;background:var(--cyan)}}.history-lane.marked i{{background:var(--green)}}.history-line i.future{{opacity:.16}}.history-line i.diverge{{outline:3px solid var(--yellow);outline-offset:1px}}.score-lane{{border-top:4px solid var(--green)}}.score-lane.control{{border-color:var(--cyan)}}.score-lane.natural{{border-color:var(--violet)}}.score-lane.compare{{border-color:var(--yellow)}}.score-lane .z{{font:760 2rem/1 ui-monospace,monospace;margin:11px 0}}.score-lane .count{{font:700 .9rem ui-monospace,monospace;color:var(--muted)}}.cutoff{{height:8px;background:var(--track);position:relative;margin-top:15px}}.cutoff::after{{content:"z > 3";position:absolute;left:75%;top:-22px;color:var(--yellow);font:700 .72rem ui-monospace,monospace}}.token-box{{height:260px;overflow:auto;padding:12px;background:var(--bg);border:1px solid var(--border);line-height:2.1}}.token{{display:inline-block;padding:0 4px;margin:2px 2px 2px 0;border:1px solid transparent;border-radius:3px;font:12px ui-monospace,monospace;white-space:pre-wrap}}.token.g{{background:color-mix(in srgb,var(--green) 20%,transparent);border-color:var(--green)}}.token.r{{background:color-mix(in srgb,var(--coral) 15%,transparent);border-color:var(--coral)}}.token.u{{color:var(--muted);border-style:dashed;border-color:var(--muted)}}.token.future{{opacity:.15}}.token.cursor{{outline:3px solid var(--yellow);outline-offset:2px}}.math{{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:18px}}.math div{{padding:13px;border-top:3px solid var(--cyan);background:var(--surface)}}.math b{{display:block;font:700 1.15rem ui-monospace,monospace}}.prefix-track{{display:grid;grid-template-columns:repeat(5,1fr);gap:5px;margin:18px 0}}.prefix-track button{{min-width:0}}.chart{{height:390px;border:1px solid var(--border);background:var(--surface);margin-top:14px}}svg{{width:100%;height:100%;display:block}}.table-wrap{{overflow:auto}}table{{width:100%;border-collapse:collapse;font-size:.86rem;min-width:760px}}th,td{{padding:9px;border-bottom:1px solid var(--border);text-align:left}}th{{color:var(--muted)}}.warning{{border-left:4px solid var(--coral)}}.good{{border-left:4px solid var(--green)}}details{{margin-top:14px;padding:0 16px;border:1px solid var(--border);background:var(--surface)}}summary{{padding:14px 0;cursor:pointer;font-weight:700}}footer{{padding:35px 0 70px;color:var(--muted)}}.static{{display:none}}noscript .static{{display:block}}
@media(max-width:800px){{body{{font-size:16px}}header,main,footer{{width:min(100% - 24px,1120px)}}section{{padding:52px 0}}.grid2,.grid4,.history,.fork{{grid-template-columns:1fr}}.math{{grid-template-columns:1fr 1fr}}.frozen{{grid-template-columns:repeat(12,1fr)}}.chart{{height:330px}}}}
@media(max-width:460px){{.math{{grid-template-columns:1fr}}.prefix-track{{grid-template-columns:1fr 1fr}}}}
@media(prefers-reduced-motion:reduce){{*,*::before,*::after{{animation-duration:.01ms!important;animation-iteration-count:1!important;scroll-behavior:auto!important;transition-duration:.01ms!important}}}}
</style><noscript><style>.static{{display:block}}</style></noscript>
</head>
<body>
<header><div class="top"><span class="stage">Text watermarking lab / Stage 7</span><button id="theme" type="button">Use light page</button></div>
<h1>One marked output has to beat three different explanations.</h1>
<p class="lede">Stage 6 froze the prompts and measured the checker's background. Stage 7 uses each prompt twice, keeps every row, and asks whether the marked path carries key-specific evidence as copied text grows.</p>
<div class="question"><strong>One question.</strong> Does the mark separate from the paired model output, the recorded natural-web continuation, and the same marked text checked with another key?</div>
<nav><a href="#frozen">Frozen row</a><a href="#pair">Paired calls</a><a href="#score">One score</a><a href="#four">Four questions</a><a href="#length">More text</a><a href="#cohort">Every row</a></nav></header>
<main>
<section id="frozen"><div class="eyebrow">Continue from Stage 6</div><h2>The 24 prompts were already in the drawer.</h2>
<p>Stage 6 selected calibration first, then froze these 24 rows in manifest order. Stage 7 used every one. The outlined square is rank 1000, our teaching row. We chose it before any generation existed.</p><div class="frozen" aria-label="Twenty-four frozen Stage 7 rows">{"".join("<span></span>" for _ in range(24))}</div>
<div class="grid2"><article class="panel"><h3>Same row identity</h3><p class="metric">1000 <small>manifest rank</small></p><p>Dataset row {spine["dataset_row_index"]}, hash <span style="font-family:monospace">{text(spine["text_sha256"][:16])}...</span></p></article><article class="panel"><h3>Stage 6 object continuity</h3><div class="token-strip">{"".join('<i class="block"></i>' for _ in range(50))}</div><p>Exact source token IDs 0 through 49. Decode and re-encode returned the same 50 IDs.</p></article></div>
<p><strong>Recorded prompt.</strong> {text(spine["source_prompt_text"])}</p></section>
<section id="pair"><div class="eyebrow">One causal change</div><h2>Fork one prompt into two generation calls.</h2>
<p>The same model, rendered prompt, seed, sampler, and safety cap enter both calls. The marked call alone receives the watermark configuration.</p><div class="fork"><div class="panel"><div class="token-strip">{"".join('<i class="block"></i>' for _ in range(50))}</div><div class="fixed"><span class="chip">seed {spine["seed"]}</span><span class="chip">temp 0.8</span><span class="chip">top-k 40</span><span class="chip">top-p .95</span><span class="chip">cap 400</span></div></div><div class="paths"><div class="path"><strong>Control Gemma call</strong><p>No keyed score change during sampling.</p></div><div class="path marked"><strong>Marked Gemma call</strong><p><span class="chip diff">watermarking_config</span> enters before each token draw.</p></div></div></div>
<div class="try"><strong>Try this.</strong> Compare the exact calls. Everything fixed will fade. Watch the single remaining argument.</div><button id="compareCalls" type="button">Compare calls</button><pre id="calls">control = model.generate(input_ids, seed, temp=.8, top_k=40, top_p=.95, max_new_tokens=400)\nmarked  = model.generate(input_ids, seed, temp=.8, top_k=40, top_p=.95, max_new_tokens=400, watermarking_config=profile)</pre><div class="feedback" id="callFeedback" aria-live="polite">The full calls are visible.</div>
<h3 style="margin-top:30px">The paths share an input, then keep their own history.</h3><p>Press Play. We stop calling tokens matched after the first different copied token. Later model scores depend on different prior histories.</p><div class="controls"><button id="play">Play</button><button id="pause">Pause</button><button id="prev">Previous</button><button id="next">Next</button><button id="replay">Replay</button></div><div class="history"><div class="history-lane"><h3>Control, achieved 198 copied tokens</h3><div class="history-line" id="controlHistory"></div></div><div class="history-lane marked"><h3>Marked, achieved 392 copied tokens</h3><div class="history-line" id="waterHistory"></div></div></div><div class="feedback" id="historyFeedback">Both outputs begin from the same frozen prompt.</div>
<p><strong>Cap and outcome stay separate.</strong> Both calls had a 400 generated-token safety cap. The copied continuations contained 198 and 392 tokens. No result page may label these as two 400-token outputs.</p></section>
<section id="score"><div class="eyebrow">Concrete numbers before notation</div><h2>Check 160 copied tokens from the fixed marked path.</h2><p>The first token supplies context. Each later token is green or red for the generation key and exact previous token. Green means keyed membership only.</p>
<div class="controls"><button id="tokenPlay">Play</button><button id="tokenPause">Pause</button><button id="tokenPrev">Previous token</button><button id="tokenNext">Next token</button><button id="tokenReplay">Replay</button></div><div class="token-box" id="tokenBox"></div><div class="feedback" id="tokenFeedback">The first copied token is unscored context.</div>
<div class="math"><div><span>Ordinary hits</span><b>{expected:.2f}</b><small>159 x 0.25</small></div><div><span>Recorded hits</span><b>58</b><small>among 159 checks</small></div><div><span>Usual movement</span><b>{movement:.4f}</b><small>sqrt(159 x .25 x .75)</small></div><div><span>Standardized distance</span><b>3.3424</b><small>(58 - 39.75) / {movement:.4f}</small></div></div><p>The standardized distance is called a z score. This row is 3.3424 usual movements above the configured quarter-green average. The separate decision rule is strict <strong>z greater than 3</strong>.</p></section>
<section id="four"><div class="eyebrow">One row, four questions</div><h2>No single control can do all three jobs.</h2><p>Keep row 1000 and the 160-token prefix fixed. Change the checked text or key one at a time.</p><div class="controls" id="familyButtons"><button data-family="watermarked_correct" class="active">1. Marked, right key</button><button data-family="control_correct">2. Model control</button><button data-family="natural_correct">3. Natural web</button><button data-family="watermarked_comparison">4. Another key</button></div><div class="grid4" id="scoreLanes">
<article class="panel score-lane" data-lane="watermarked_correct"><h3>Marked / right key</h3><div class="z">3.3424</div><div class="count">58 / 159</div><p>Did keyed generation leave correct-key evidence?</p><div class="cutoff"></div></article>
<article class="panel score-lane control" data-lane="control_correct"><h3>Model control / right key</h3><div class="z">1.3278</div><div class="count">47 / 159</div><p>Would ordinary Gemma output score high?</p><div class="cutoff"></div></article>
<article class="panel score-lane natural" data-lane="natural_correct"><h3>Natural web / right key</h3><div class="z">0.5952</div><div class="count">43 / 159</div><p>Would the recorded source domain score high?</p><div class="cutoff"></div></article>
<article class="panel score-lane compare" data-lane="watermarked_comparison"><h3>Marked / another key</h3><div class="z">-1.9688</div><div class="count">29 / 159</div><p>Is the evidence tied to this key?</p><div class="cutoff"></div></article></div><div class="feedback" id="familyFeedback">Marked text with the generation key scores 58/159, z 3.3424. It crosses the frozen cutoff.</div></section>
<section id="length"><div class="eyebrow">Grow the same history</div><h2>More copied text adds checks. It does not promise a smooth climb.</h2><p>Each button rechecks a longer prefix of the same recorded outputs. Existing token identities and order remain fixed. Unsupported prefixes stay absent.</p><div class="prefix-track"><button data-prefix="40">40</button><button data-prefix="80">80</button><button data-prefix="160" class="active">160</button><button data-prefix="200">200</button><button data-prefix="400" disabled>400</button></div><div class="chart"><svg id="prefixSvg" role="img" aria-labelledby="prefixTitle prefixDesc"><title id="prefixTitle">Spine row z by copied-token prefix</title><desc id="prefixDesc">Four score families at 40, 80, and 160 copied tokens. Longer points are unavailable when the paired control ends.</desc></svg></div><div class="feedback" id="prefixFeedback">At 160 copied tokens, marked correct-key z is 3.3424. The paired control ends at 198 copied tokens, so this row has no 200 or 400 point.</div>
<div class="panel warning" style="margin-top:24px"><h3>The predeclared inconvenient row stayed</h3><p>Rank 1001's control and marked paths shared their first 80 copied token IDs. Both scored <strong>26/79, z 1.6239</strong> with the generation key. Changing token probabilities did not force a different early draw.</p><button id="showEqual" type="button">Inspect the equal prefix</button><div class="feedback" id="equalFeedback">The row was selected by the frozen rule, not by taste.</div></div></section>
<section id="cohort"><div class="eyebrow">Rows before averages</div><h2>Reveal every paired difference before the mean.</h2><p>A dot is one document. Zero means equal z. A positive value means the marked correct-key score was higher. The cohort changes only when both generated paths fail to reach a longer prefix.</p><div class="controls" id="contrastButtons"><button data-contrast="versus_control" class="active">Versus model control</button><button data-contrast="versus_natural">Versus natural web</button><button data-contrast="versus_comparison_key">Versus another key</button></div><div class="controls" id="cohortPrefixes"><button data-cprefix="40">40</button><button data-cprefix="80" class="active">80</button><button data-cprefix="160">160</button><button data-cprefix="200">200</button><button data-cprefix="400" disabled>400</button></div><div class="chart"><svg id="differenceSvg" role="img" aria-labelledby="diffTitle diffDesc"><title id="diffTitle">Document-level paired z differences</title><desc id="diffDesc">Every complete row appears as a dot around a zero line. The mean and paired bootstrap interval are layered above them.</desc></svg></div><div class="feedback" id="cohortFeedback"></div>
<div class="table-wrap"><table><thead><tr><th>Copied prefix</th><th>Complete pairs</th><th>Marked cutoff crossings</th><th>Model control</th><th>Natural web</th><th>Another key</th></tr></thead><tbody>
{"".join(f"<tr><td>{p}</td><td>{summaries[str(p)]['complete_rows']}</td><td>{summaries[str(p)]['cutoff_counts']['watermarked_correct']}</td><td>{summaries[str(p)]['cutoff_counts']['control_correct']}</td><td>{summaries[str(p)]['cutoff_counts']['natural_correct']}</td><td>{summaries[str(p)]['cutoff_counts']['watermarked_comparison']}</td></tr>" for p in (40, 80, 160, 200, 400))}
</tbody></table></div>
<p><strong>The missing 400 row is a result.</strong> No pair reached 400 copied tokens in both generated conditions. We report zero complete pairs, not a zero effect.</p></section>
<section><div class="eyebrow">Measured answer</div><h2>The average separated. Individual rows still overlapped.</h2><div class="grid2"><article class="panel good"><h3>At 80 copied tokens, all 24 rows</h3><p class="metric">+1.8296 <small>z versus model control</small></p><p>95 percent paired bootstrap interval [1.3424, 2.3276]. Versus natural web: +1.7538 [1.3100, 2.1977]. Versus another key: +2.0461 [1.6131, 2.4792].</p></article><article class="panel warning"><h3>What the mean does not erase</h3><p>Rank 1001 was equal to its model control at 80 tokens. One natural-web row crossed at 200. Many marked rows stayed below strict z greater than 3.</p></article></div>
<p>The narrow result is measured separation among four configured score families in one pinned 24-row Gemma experiment. A positive row means only <strong>consistent with this configured watermark and key</strong>.</p>
<div class="panel"><h3>Still open</h3><p>This stage does not establish generic AI detection, human authorship, production error rates, prose quality, editing robustness, another model or key, or Claude's private implementation. Stage 8 has not started.</p></div>
<details><summary>Runtime and reproducibility</summary><p>Source commit <code>{artifact["source_commit"]}</code>. Config SHA-256 <code>{artifact["config_sha256"]}</code>. The one L4 function completed in {runtime_seconds:.1f} seconds and returned {artifact["generated_token_id_count"]:,} generated token IDs. At the configured L4 rate, that is ${gpu_cost:.4f} of GPU time. It is not the provider's total bill.</p></details><details><summary>Scripts-off summary</summary><p>At 80 copied tokens, all 24 pairs were complete. Mean marked correct-key z exceeded model control by 1.8296, natural web by 1.7538, and comparison key by 2.0461. No pair supported a 400 copied-token comparison.</p></details><div class="static panel"><h3>Scripts-off fallback</h3><p>The complete evidence table and measured answer above remain readable. Interactive charts require JavaScript.</p></div></section>
</main><footer>Measured values come from <code>artifacts/lab-07/results.json</code>. No external font, script, storage, model, or network call is required by this page.</footer>
<script>
const DATA={compact(payload)};const $=id=>document.getElementById(id);const NS='http:'+'//www.w3.org/2000/svg';
$('theme').onclick=()=>{{const light=document.documentElement.dataset.theme==='light';document.documentElement.dataset.theme=light?'dark':'light';$('theme').textContent=light?'Use light page':'Use dark page';}};
let compactCalls=false;$('compareCalls').onclick=()=>{{compactCalls=!compactCalls;$('calls').textContent=compactCalls?'fixed: model, input_ids, seed, temp=.8, top_k=40, top_p=.95, max_new_tokens=400\\nchanged: watermarking_config=profile':'control = model.generate(input_ids, seed, temp=.8, top_k=40, top_p=.95, max_new_tokens=400)\\nmarked  = model.generate(input_ids, seed, temp=.8, top_k=40, top_p=.95, max_new_tokens=400, watermarking_config=profile)';$('callFeedback').textContent=compactCalls?'One generation-time argument changes. No text is marked after sampling.':'The full calls are visible.';}};
const ci=DATA.spineControlIds,wi=DATA.spineWaterIds,first=ci.findIndex((v,i)=>wi[i]!==v);let hi=0,ht=null,motion=matchMedia('(prefers-reduced-motion: reduce)').matches;for(let i=0;i<80;i++){{for(const [id,arr] of [['controlHistory',ci],['waterHistory',wi]]){{const el=document.createElement('i');if(i>0)el.className='future';if(i===first)el.classList.add('diverge');el.title=`position ${{i}}, ID ${{arr[i]}}`;$(id).append(el);}}}}function renderHistory(){{for(const id of ['controlHistory','waterHistory']) [...$(id).children].forEach((e,i)=>e.classList.toggle('future',i>hi));$('historyFeedback').textContent=hi<first?`Position ${{hi}}: the two recorded paths still share this copied prefix.`:hi===first?`Position ${{first}} is the first different copied token. Stop matching later token objects.`:`Position ${{hi}}: each path now conditions on its own sampled history.`;if(hi>=79)pauseH();}}function pauseH(){{if(ht)clearInterval(ht);ht=null}}function playH(){{if(motion){{$('historyFeedback').textContent='Reduced motion is active. Use Previous and Next.';return}}pauseH();ht=setInterval(()=>{{hi=Math.min(79,hi+1);renderHistory()}},90)}}$('play').onclick=playH;$('pause').onclick=pauseH;$('next').onclick=()=>{{pauseH();hi=Math.min(79,hi+1);renderHistory()}};$('prev').onclick=()=>{{pauseH();hi=Math.max(0,hi-1);renderHistory()}};$('replay').onclick=()=>{{pauseH();hi=0;renderHistory();playH()}};renderHistory();
const box=$('tokenBox');DATA.tokens.slice(0,160).forEach(t=>{{const e=document.createElement('span');e.className='token '+(t.eligible?(t.is_green?'g':'r'):'u')+' future';e.textContent=t.piece||' ';e.title=`position ${{t.position}}, ID ${{t.token_id}}, ${{t.eligible?(t.is_green?'green':'red'):'unscored'}}`;box.append(e)}});let ti=0,tt=null;function pauseT(){{if(tt)clearInterval(tt);tt=null}}function renderT(){{[...box.children].forEach((e,i)=>{{e.classList.toggle('future',i>ti);e.classList.toggle('cursor',i===ti)}});const shown=DATA.tokens.slice(0,ti+1),eligible=shown.filter(t=>t.eligible),g=eligible.filter(t=>t.is_green).length;$('tokenFeedback').textContent=ti===0?'The first copied token is unscored context.':`Through position ${{ti}}, ${{g}} of ${{eligible.length}} eligible tokens are green.`;box.children[ti].scrollIntoView({{block:'nearest'}});if(ti>=159)pauseT()}}function playT(){{if(motion){{$('tokenFeedback').textContent='Reduced motion is active. Use manual token steps.';return}}pauseT();tt=setInterval(()=>{{ti=Math.min(159,ti+1);renderT()}},55)}}$('tokenPlay').onclick=playT;$('tokenPause').onclick=pauseT;$('tokenNext').onclick=()=>{{pauseT();ti=Math.min(159,ti+1);renderT()}};$('tokenPrev').onclick=()=>{{pauseT();ti=Math.max(0,ti-1);renderT()}};$('tokenReplay').onclick=()=>{{pauseT();ti=0;renderT();playT()}};renderT();
const familyCopy={{watermarked_correct:'Marked text with the generation key scores 58/159, z 3.3424. It crosses the frozen cutoff.',control_correct:'The paired model control scores 47/159, z 1.3278. Same source prompt and seed, no watermark processor.',natural_correct:'The recorded natural-web continuation scores 43/159, z 0.5952. This checks source-domain variation.',watermarked_comparison:'The same marked text with another key scores 29/159, z -1.9688. This checks key specificity.'}};document.querySelectorAll('[data-family]').forEach(b=>b.onclick=()=>{{document.querySelectorAll('[data-family]').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('[data-lane]').forEach(x=>x.style.opacity=x.dataset.lane===b.dataset.family?'1':'.38');$('familyFeedback').textContent=familyCopy[b.dataset.family];}});
function svg(tag,a={{}}){{const e=document.createElementNS(NS,tag);for(const [k,v] of Object.entries(a))e.setAttribute(k,v);return e}}const colors={{watermarked_correct:'var(--green)',control_correct:'var(--cyan)',natural_correct:'var(--violet)',watermarked_comparison:'var(--yellow)'}};function prefixChart(active){{const root=$('prefixSvg');root.replaceChildren();root.setAttribute('viewBox','0 0 900 380');const ps=[40,80,160],x=p=>80+(ps.indexOf(p))*340,y=z=>30+(5-z)/8*290;[-2,0,3].forEach(v=>{{root.append(svg('line',{{x1:55,x2:860,y1:y(v),y2:y(v),stroke:v===3?'var(--yellow)':'var(--border)','stroke-dasharray':v===3?'7 5':'none'}}));const t=svg('text',{{x:10,y:y(v)+4,fill:'var(--muted)','font-size':'13'}});t.textContent=`z ${{v}}`;root.append(t)}});for(const fam of Object.keys(colors)){{let d='';for(const p of ps){{const z=DATA.rows[0].prefix[p][fam].z_score;d+=(d?'L':'M')+x(p)+','+y(z)}}root.append(svg('path',{{d,fill:'none',stroke:colors[fam],'stroke-width':fam==='watermarked_correct'?'4':'2'}}));for(const p of ps)root.append(svg('circle',{{cx:x(p),cy:y(DATA.rows[0].prefix[p][fam].z_score),r:p===active?'7':'4',fill:colors[fam]}}))}}for(const p of ps){{const t=svg('text',{{x:x(p),y:355,fill:'var(--muted)','font-size':'13','text-anchor':'middle'}});t.textContent=p+' copied tokens';root.append(t)}}}}document.querySelectorAll('[data-prefix]').forEach(b=>b.onclick=()=>{{const p=Number(b.dataset.prefix);document.querySelectorAll('[data-prefix]').forEach(x=>x.classList.toggle('active',x===b));prefixChart(p);const s=DATA.rows[0].prefix[p];$('prefixFeedback').textContent=`At ${{p}} copied tokens: marked z ${{s.watermarked_correct.z_score.toFixed(4)}}, model control ${{s.control_correct.z_score.toFixed(4)}}, natural web ${{s.natural_correct.z_score.toFixed(4)}}, another key ${{s.watermarked_comparison.z_score.toFixed(4)}}.`;}});prefixChart(160);$('showEqual').onclick=()=>{{$('equalFeedback').textContent='The first 80 control and marked copied token IDs are identical. Both generation-key scores are 26/79, z 1.6239.';}};
let cp=80,contrast='versus_control';function differenceChart(){{const root=$('differenceSvg'),s=DATA.summaries[cp],c=s.comparisons[contrast],vals=c.row_differences;root.replaceChildren();root.setAttribute('viewBox','0 0 900 380');const min=-3,max=6,x=v=>80+(v-min)/(max-min)*760;root.append(svg('line',{{x1:x(0),x2:x(0),y1:35,y2:325,stroke:'var(--coral)','stroke-width':'2'}}));vals.forEach((v,i)=>root.append(svg('circle',{{cx:x(v),cy:60+(i%12)*20,r:i<2?'6':'4',fill:i===0?'var(--yellow)':i===1?'var(--coral)':'var(--cyan)'}})));root.append(svg('line',{{x1:x(c.interval_low),x2:x(c.interval_high),y1:315,y2:315,stroke:'var(--green)','stroke-width':'7'}}));root.append(svg('circle',{{cx:x(c.mean_difference),cy:315,r:'8',fill:'var(--green)'}}));[-2,0,2,4,6].forEach(v=>{{const t=svg('text',{{x:x(v),y:355,fill:'var(--muted)','font-size':'13','text-anchor':'middle'}});t.textContent=(v>0?'+':'')+v;root.append(t)}});$('cohortFeedback').textContent=`Prefix ${{cp}}, ${{s.complete_rows}} complete pairs. Mean difference ${{c.mean_difference.toFixed(4)}}, paired bootstrap interval [${{c.interval_low.toFixed(4)}}, ${{c.interval_high.toFixed(4)}}]. Every dot remains visible.`;}}document.querySelectorAll('[data-contrast]').forEach(b=>b.onclick=()=>{{contrast=b.dataset.contrast;document.querySelectorAll('[data-contrast]').forEach(x=>x.classList.toggle('active',x===b));differenceChart()}});document.querySelectorAll('[data-cprefix]').forEach(b=>b.onclick=()=>{{cp=Number(b.dataset.cprefix);document.querySelectorAll('[data-cprefix]').forEach(x=>x.classList.toggle('active',x===b));differenceChart()}});differenceChart();
</script></body></html>"""
    OUTPUT.parent.mkdir(parents=True, exist_ok=True)
    OUTPUT.write_text(html)
    print(f"Wrote {OUTPUT} ({OUTPUT.stat().st_size} bytes)")


if __name__ == "__main__":
    main()
